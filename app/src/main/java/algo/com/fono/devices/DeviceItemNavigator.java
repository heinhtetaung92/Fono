package algo.com.fono.devices;

/**
 * Defines the navigation actions that can be called from a list item in the task list.
 */
public interface DeviceItemNavigator {

    void openDeviceDetails(int deviceId);
}
